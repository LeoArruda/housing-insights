{
  "name": "@housing-insights/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/main.ts",
    "start": "bun run src/main.ts",
    "cli": "bun run src/cli.ts",
    "daemon": "bun run src/daemon.ts",
    "migrate": "bun run src/cli.ts migrate",
    "test": "bun test"
  },
  "dependencies": {
    "@housing-insights/types": "workspace:*",
    "croner": "^9.0.0",
    "fast-xml-parser": "^4.5.0",
    "hono": "^4.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
